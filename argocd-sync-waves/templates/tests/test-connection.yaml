apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "argocd-sync-waves.fullname" . }}-test-connection"
  labels:
    {{- include "argocd-sync-waves.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "argocd-sync-waves.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
